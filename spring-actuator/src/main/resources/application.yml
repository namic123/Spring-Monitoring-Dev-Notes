management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  server:
    port: 8080
  endpoint:
   shutdown:
      enabled: true
   health:
     show-components: always # 이 부분이 다름
  #     show-details: always
  endpoints:
    web:
      exposure:
        include: "*"

info:
  app:
    name: hello-actuator
    company: Jay

#logging:
#  level:
#    hello.controller: debug

server:
  tomcat:
    mbeanregistry:
      enabled: true